# /faq/* Consolidation Plan — sanctionsai.dev (order #3: PRUNE 436 → 67 hubs)

Status: **PLAN READY — awaiting owner approval (§5.6b) before any 301 ships.**

## The problem
436 on-disk FAQ pages under `faq/*/index.html`, served by the filesystem prefix
loop in `api.py`. Measured 2026-08-15:

| metric | value |
|---|---|
| total pages | 436 |
| median body chars | 880 |
| pages < 1,000 chars | 369 (85%) |
| pages ≥ 2,500 chars | 2 |

Three clean patterns:

1. **200 "Part N" guide fragments** — 20 series × 10 parts
   (`ofac-screening-{api,mcp,risk,x402,crewai,fincen,openai,solana,autogen,bitcoin,fintech,polygon,startup,agentkit,ethereum,treasury,developer,langchain,enterprise,regulation}-guide-{1..10}`).
   Verified byte-identical except the "Part N" label. Same fragmentation the
   commit 76111e7 already collapsed for the `-faq-N` variant.
2. **~150 "OFAC screening for X explained / what-is-…-for-X" stubs** — 358–900 chars.
3. **~40 standalone Q&A** — 1,000–3,100 chars; several are 5-way duplicates
   ("how often is the SDN list updated" is one question asked as 5 URLs).

## The map
Generated by `scripts/build_faq_consolidation_map.py` →
`scripts/faq_consolidation_map.json`:

- **keep (67 hubs):** 19 existing "OFAC X FAQs" topic pages + 20 guide-part-1
  seeds + 28 substantive standalone Q&A.
- **redirect (369 stubs):** each 301s to its hub. 200 are the guide-part-2..10
  fragments; the rest are keyword/duplicate-matched to the nearest hub.
- 64 hubs are still < 1,800 chars and are flagged `targets_needing_build_ge1800`
  — they must be deepened (order #4 territory) in lockstep, but the redirects
  themselves only remove thin duplicate URLs.

## Execution (gated by §5.6 — NOT started)
1. **Rollback baseline:** tag `pre-faq-prune` at the pre-change HEAD (done when
   this plan commits).
2. **Owner approval** — required before any 301 ships.
3. **Batch 1 (canary, 50 URLs, held 7 days):** the 20 guide series' part-2..3
   fragments (40 URLs) + 10 lowest-risk stubs.
4. **Remaining batches:** ≤500 URLs/week/site. Total 369 redirects + 369 stub
   file deletions + sitemap update, across ~2 weeks.
5. **Mechanics:** add `/faq/<stub>` → `/faq/<hub>` entries to the
   `HALLUCINATED_REDIRECTS` dict in `api.py` (301, `Cache-Control` 86400), delete
   the stub `index.html`, remove stubs from the sitemap `URL_LASTMOD` dict.
   `py_compile` gate before every deploy; `deploy_from_commit.sh`, never the
   working tree.

## Guardrail compliance notes
- §5.6: rollback tag + owner approval + canary + weekly cap — see above.
- §5.7: no net-new URLs. This order only removes/redirects existing URLs.
- §5.1: no new statistics introduced; hub deepening (order #4) carries the
  sourcing requirement.

Generated: 2026-08-15. Owner of record: Maryan Kindrat.
